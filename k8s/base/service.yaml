apiVersion: v1
kind: Service
metadata:
  name: web-client
  namespace: web-client
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: web-client
